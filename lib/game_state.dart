import 'package:flutter/material.dart';

class GameState {
  // Strict game related state:
  Offset bluePos = Offset.zero;
  Offset? redPos;

  // Input state:
  // ...
  // Key up pressed
  // Mouse clicked at xy (optional)
  // Touchpad?
}
